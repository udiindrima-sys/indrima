 # include <stdio.h>
 void main()
 {
     int a,b;
    printf("Enter any two numbers: ");
    scanf("%d%d",&a,&b);
    
    int big=a>b? a:b ;
    int small=a<b? a:b;
    int step=big;
    
    while (big%small !=0);
    
    {
        printf("LCM is %d", big); 
    }
 }
